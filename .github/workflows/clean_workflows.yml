name: Delete workflow runs
on:
  workflow_dispatch:
#  schedule:
#    - cron: "10 12 * * *"
jobs:
  build:   
    runs-on: ubuntu-latest
    steps:
    
      - name: Delete workflow runs
        uses: Mattraks/delete-workflow-runs@v2
        with:
          retain_days: 0
          keep_minimum_runs: 2
